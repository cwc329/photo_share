# ── 開發環境（Vite dev server） ───────────────────────────────
# 直接呼叫本地後端，不走 proxy
# 後端 CORS 已設定允許 http://localhost:5173
VITE_API_BASE_URL=http://localhost:8001

# ── 正式環境（Docker build） ──────────────────────────────────
# VITE_API_BASE_URL=
